<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.teamtest_ver10">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">



        <activity
            android:name=".Main_TabActivity_Tabhost"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



        <activity android:name=".TabFirstActivity" />
        <activity android:name=".TabSecondActivity"/>
        <activity android:name=".TabThirdActivity"/>
        <activity android:name=".ReleaseActivity" />
        <activity android:name=".TabThirdActivity_offline"/>
        <activity android:name=".ActivityDetail"/>

    </application>

    <!-- 互联网 -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- 查看网络状态 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

</manifest>